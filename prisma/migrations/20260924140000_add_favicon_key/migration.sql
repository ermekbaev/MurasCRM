-- Свой значок вкладки.
--
-- Рисованная буква на плашке — разумный запас, когда у клиента ничего нет. Но
-- если у него уже есть фавикон на сайте, вкладка CRM должна выглядеть так же:
-- иначе в браузере рядом висят две вкладки одной компании с разными значками.
ALTER TABLE "CompanySettings" ADD COLUMN "faviconKey" TEXT;
